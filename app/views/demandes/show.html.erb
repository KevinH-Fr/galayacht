<p style="color: green"><%= notice %></p>

<%= render @demande %>

<div>
  <%= link_to "Edit this demande", edit_demande_path(@demande) %> |
  <%= link_to "Back to demandes", demandes_path %>

  <%= button_to "Destroy this demande", @demande, method: :delete %>
</div>
