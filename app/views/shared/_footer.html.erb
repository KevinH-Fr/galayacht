

<div class="footer text-light">   
    <% if current_user.present? %>
        <%= link_to '', user_path(current_user.id), class: "fa-regular fa-user btn btn-light" %>

        <% if user_admin.present? %>
            Admin

        <% elsif user_preneur.present? %>
            <%= link_to user_path(current_user.id), class: "no-underline fs-6" do %>
                Locataire
            <% end %>
        <%# elsif user_bailleur.nil? %>
            <%#= link_to "Créer profile locataire", new_preneur_path, class: "text-center no-underline text-warning" %>
        <%# end %>
        <% elsif user_bailleur.present? %>
            <%= link_to user_path(current_user.id), class: "no-underline" do %>
                Bailleur
            <% end %>
         <%# elsif user_preneur.nil? %>
            <%#= link_to "Créer profile bailleur", new_bailleur_path, class: "text-center no-underline text-primary" %>
        <% end %>
        <%= button_to "", destroy_user_session_path, method: :delete, class: "fa-solid fa-right-from-bracket btn btn-danger"  %>
    <% else %>
        <%= link_to '', new_user_session_path, 
            class: "fa-solid fa-user-lock btn btn-warning",
            alt: "Sign in",
            'aria-label' => "Se connecter au compte" %>
        <%= link_to '', new_user_registration_path, class: "fa-solid fa-user-plus btn btn-primary" %>
    <% end %>  
</div>
