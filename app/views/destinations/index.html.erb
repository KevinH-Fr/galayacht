<% description "Retrouvez toutes nos destinations et lieux de départ pour louer un bateau ou un yacht à Antibes, Cannes, Mandelieu, et sur la Côte d'Azur. Louez le bateau de vos rêves dès aujourd'hui." %>

<div id="destinations" class="main-card mt-4">
  <div class="row justify-content-center">
    <% @destinations.each do |destination| %>
      <div class="col-sm-6 col-md-4 col-lg-3 d-flex align-items-center justify-content-center mb-3">
        <%= render destination %>
      </div>
    <% end %>  
  </div>
</div>

<% if current_user.present? && current_user.admin? %>
  <%= link_to "Nouvelle destination", new_destination_path, class: "btn btn-primary" %>
<% end %>