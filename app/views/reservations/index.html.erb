<p style="color: green"><%= notice %></p>

<h1>Reservations</h1>

<div id="reservations">
  <% @reservations.each do |reservation| %>
    <%= render reservation %>
  <% end %>
</div>

<hr>

<%= link_to "New reservation", new_reservation_path, class: "btn btn-warning" %>
